const MinTop = () => {
  const style = {
    width: '100%',
    height: '100%',
    background: 'black',
    color: 'white',
    fontSize: '0.2rem',
  };
  return <div style={style}>MinTop</div>;
};

export default MinTop;
